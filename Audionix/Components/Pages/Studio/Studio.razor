@page "/studio"
@rendermode InteractiveServer

@using MudBlazor
@using MudBlazor.Components

<PageTitle>Live Studio</PageTitle>

<MudPaper>
    <MudToolBar Dense="false" Variant="Variant.Outline">
        <MudPaper Class="mx-auto">
            <MudButton Class="ma-1" Variant="Variant.Outlined" OnClick="ToggleInsertDrawer">Insert</MudButton>
            <MudButton Class="ma-1" Variant="Variant.Outlined" OnClick="ToggleDelete">Delete</MudButton>
        </MudPaper>
    </MudToolBar>
</MudPaper>

<MudPaper>
    <MudDrawerContainer>
        <MudDrawer @bind-Open="@_open" Variant="DrawerVariant.Temporary" OverlayAutoClose="true" Width="400px" Anchor="Anchor.Right" Color="Color.Default">
            <MudPaper>
                <MudDrawerHeader>
                    <MudText Typo="Typo.h6">Add Audio</MudText>
                </MudDrawerHeader>

                <MudSelect Class="pl-1 pr-1" Margin="Margin.Dense" T="Folder" Label="Folders" Value="selectedFolder" Variant="Variant.Outlined" ValueChanged="OnFolderChanged">
                    @foreach (var folder in Folders)
                    {
                        <MudSelectItem Value="@folder">@folder.Name</MudSelectItem>
                    }
                </MudSelect>

                <MudList T="string" Class="">
                    @foreach (var audioFile in AudioFiles)
                    {
                        <MudListItem Class="" @onclick="() => SelectAudioFile(audioFile)">
                            @audioFile.Title - @audioFile.Artist
                        </MudListItem>
                    }
                </MudList>
            </MudPaper>
        </MudDrawer>
    </MudDrawerContainer>

    <MudDataGrid Items="ProgramLog"
                 RowStyleFunc="@RowStyleFunc"
                 SortMode="SortMode.Single"
                 FixedHeader="true"
                 Virtualize="true"
                 ShowMenuIcon="false"
                 Dense="true"
                 Height="90vh">
        <Columns>
            <PropertyColumn Title="ID" Property="x => x.LogOrderID" SortBy="@(x => x.LogOrderID)" CellStyle="width: 100px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;" />
            <PropertyColumn Property="x => x.Title" Sortable="false" />
            <PropertyColumn Property="x => x.Artist" Sortable="false" />
            <PropertyColumn Property="x => x.TimeScheduled" Sortable="false" CellStyle="width: 100px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;" />
            <PropertyColumn Property="x => x.Length" Sortable="false" CellStyle="width: 100px;" />
            <PropertyColumn Property="x => x.Date" Sortable="false" />
            @if (IsAudioFileSelected)
            {
                <TemplateColumn Sortable="false">
                    <CellTemplate Context="cellContext">
                        <MudButton Variant="Variant.Outlined" OnClick="() => AddSelectedAudioToLog(cellContext.Item.LogOrderID, cellContext.Item)">Add</MudButton>
                    </CellTemplate>
                </TemplateColumn>
            }
            @if (_delete)
            {
                <TemplateColumn Sortable="false">
                    <CellTemplate Context="cellContext">
                        <MudButton Variant="Variant.Outlined" OnClick="() => DeleteSelectedLogItem(cellContext.Item)">Delete</MudButton>
                    </CellTemplate>
                </TemplateColumn>
            }
        </Columns>
    </MudDataGrid>
</MudPaper>
